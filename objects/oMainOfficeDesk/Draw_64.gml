/*draw_set_font(fGUI);
draw_set_halign(vk_left);

for (var i = 0; i < array_length(colourArray); i++) {
	draw_text(20, 20 + i * 10, colourArray[i]);
}

draw_text(40, 20, "colour length: " + string(array_length(colourArray)));