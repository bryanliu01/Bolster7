//If an item is used or discarded, play that item's text and effects
if (objectScript != noone) {
	CreateCutscene(objectScript);
	objectScript = noone;
}
//If there is no script assigned to it, play a default effect
//TODO

	